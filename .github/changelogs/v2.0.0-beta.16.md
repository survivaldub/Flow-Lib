## Change Logs

### Changes

* _Bootstraps_ option now uses Electron Updater instead of a custom solution.

### Bug fixes and improvements

* Fixed _News_ and _Background_ options to properly load remote content.
* `electron` and `electron-updater` are now peer dependencies. 